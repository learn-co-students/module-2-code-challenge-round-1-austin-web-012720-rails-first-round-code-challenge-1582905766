<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= link_to power.name, power_path(power) %>
      <br>
      Description: <%= power.description %><br>
      Heroines with this Power: <%= power.count_heroines_with_power%>
    </li>
    <br>
  <% end %>
</ul>

